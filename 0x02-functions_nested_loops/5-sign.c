#include "main.h"
/**
 *print_sign - prints the sign of a number
 *
 *Return: returns 1 if n>0,0 if n=0 and -1 if n<0
 */
int print_sign(int)
{
	int n;

	if (n > 0)
		_putchar('+');
	else if (n = 0)
		_putchar(0);
	else
		_putchar('-');

	return (0);
}
 
