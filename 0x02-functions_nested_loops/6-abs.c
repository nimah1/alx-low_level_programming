#include "main.h"
/**
 *_abs - computes absolute value
 *@c: parameter to be computed
 *
 *Return: always 0(success)
 */
int _abs(int c)
{
	int j;

	while (c < 0)
	{
		j = (c / -1);
	}
	return (j);
}
